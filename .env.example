# Exemplo de variáveis de ambiente - copie para .env e ajuste valores
# Nunca commitar .env com segredos reais.

# Banco de dados (Postgres)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app

# JWT
JWT_SECRET=changeme_dev_secret
JWT_EXPIRES_IN=1d

# Microserviços externos
SALES_MS_URL=http://localhost:3001
AI_RISK_MANAGER_SERVICE_URL=https://localhost:4000

# Integrações
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Análise estática / Qualidade
SONAR_TOKEN=__DEFINA_NO_GITHUB_SECRETS__

# Observação: SONAR_TOKEN e chaves Cloud devem ser definidos em GitHub Secrets, não em produção no .env versionado.
